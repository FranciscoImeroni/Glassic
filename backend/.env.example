# ==================== ENTORNO ====================
NODE_ENV=development
PORT=3000

# ==================== BASE DE DATOS ====================
# URL completa de conexión a PostgreSQL
# Formato: postgresql://usuario:password@host:puerto/nombre_db
#
# Para desarrollo local (recomendado):
DATABASE_URL=postgresql://glassic_user:tu_password_seguro@localhost:5432/glassic_db
#
# Si usas el usuario postgres por defecto:
# DATABASE_URL=postgresql://postgres:tu_password@localhost:5432/glassic_db
#
# Para Railway u otro servicio en la nube:
# DATABASE_URL=postgresql://postgres:PASSWORD@host.railway.app:5432/railway?sslmode=require

# ==================== CLOUDINARY ====================
# Servicio de almacenamiento de imágenes en la nube
# Necesario para: imágenes de productos, planos, esquemas y plantillas
#
# Obtén tus credenciales en: https://cloudinary.com/
# Dashboard > Account > API Keys
#
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret

# ==================== NOTAS ====================
# 1. Copia este archivo como .env y completa tus credenciales
# 2. NUNCA commitees el archivo .env (ya está en .gitignore)
# 3. Para producción, configura estas variables en tu servicio de hosting
# 4. Cloudinary es OBLIGATORIO para que funcionen las imágenes
